<h2>商品新規登録</h2>

　<%= form_with model: @item, url: admin_items_path do |f| %>
    <h4>商品画像</h4>
      
    <h4>商品名</h4>
      <%= f.text_field :name %>
    <h4>商品説明</h4>  
      <%= f.text_area :introduction %>
    <h4>ジャンル</h4>
      <%= f.collection_select(:genre_id,@genres.id)  include_blank: "--選択して下さい--" %>
      <%= f.collection_select :保存先のカラム名, テーブル（モデル）名.all, :実際にDBに保存させるカラム名, :選択肢に用いるカラム名,
    <h4>税抜価格</h4>
    
    <h4>販売ステータス</h4>
    
     <%= f.submit '新規登録' %>
   <% end %>
<h2>商品一覧</h2>
 <p>（全 <%= @item.count %> 件）</p>


<% @items.each do |item| %>


        <td>
          <%= image_tag item.image, size: "50x50" %>
          <%= link_to item.name, item_path(item.id) %>
          <%= item.price %>
        </td>
   
 　  
<% end %>



<%= paginate @items %>